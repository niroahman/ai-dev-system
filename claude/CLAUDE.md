At the start of every session, check for `.agent-context.md` in the working
directory and read it if present. It contains my personal skills layered on
top of any AGENTS.md the repo provides. Repo AGENTS.md takes precedence on
conflicts.

Also check for:

- `.agent-task.md` — current task spec, follow this
- `.investigate-prompt.md` — if present, follow it exactly. This is an
  investigation worktree (operating as Nikke). Do not modify code.

If a task file or prompt assigns you a role name (Pat, Mat, Nikke, Poirot),
operate as that role. The role names live alongside character portraits
that may appear in markdown files you generate — include them when the
prompt says to.
